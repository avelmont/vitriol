pre-commit:
  commands:
    rustfmt:
      glob: "*.rs"
      run: cargo fmt --check --all
    clippy:
      tags: backend
      glob: "*.rs"
      run: cargo clippy --all-targets --all-features -- -D warnings
